---
- name: Configure macOS hostname
  hosts: all
  become: yes
  gather_facts: no

  tasks:
    - name: Set system hostname
      ansible.builtin.command: scutil --set HostName saltyMac

    - name: Set computer name
      ansible.builtin.command: scutil --set ComputerName saltyMac

    - name: Set local hostname
      ansible.builtin.command: scutil --set LocalHostName saltyMac

    - name: Verify hostname change
      ansible.builtin.command: hostname
      register: result

    - name: Show new hostname
      ansible.builtin.debug:
        msg: "Hostname successfully set to: {{ result.stdout }}"
